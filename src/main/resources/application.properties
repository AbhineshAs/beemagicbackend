spring.application.name=backend

# Default active profile (Railway production default)
spring.profiles.active=${SPRING_PROFILES_ACTIVE:prod}

# ADMIN CONFIGURATION
# ===============================
admin.email=${ADMIN_EMAIL:admin@beemagic.com}
admin.password=${ADMIN_PASSWORD:beemagic@be123}

# MAIL CONFIGURATION
# ===============================
spring.mail.host=${SPRING_MAIL_HOST:smtp.gmail.com}
spring.mail.port=${SPRING_MAIL_PORT:587}
spring.mail.username=${SPRING_MAIL_USERNAME:beemagic777@gmail.com}
spring.mail.password=${SPRING_MAIL_PASSWORD:okxcoysbgxhqtkmm}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# APP SETTINGS
# ===============================
app.frontend-url=${APP_FRONTEND_URL:https://beemagic.in}

# RAZORPAY CONFIGURATION
# ===============================
razorpay.key.id=${RAZORPAY_KEY_ID:}
razorpay.key.secret=${RAZORPAY_KEY_SECRET:}
